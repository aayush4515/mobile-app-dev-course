<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Navigate.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Navigate"
    xmlns:local="clr-namespace:Navigate.Pages"
    xmlns:holidays="clr-namespace:Navigate.Pages.Holidays"
    Title="Navigate App"

    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="Settings"
        ContentTemplate="{DataTemplate local:SettingsPage}"
        Route="SettingsPage" />

    <ShellContent
        Title="Thanksgiving Day"
        ContentTemplate="{DataTemplate holidays:ThanksgivingPage}"
        Route="ThanksgivingPage" />

</Shell>

